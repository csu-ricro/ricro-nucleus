import React from 'react';
import Typography from '@material-ui/core/Typography';

const Groups = () => {
  return <Typography variant="display2">Groups</Typography>;
};

Groups.propTypes = {};

export default Groups;
